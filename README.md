# Overview

This repository contains a list of the dotfiles I use for:

- alacritty terminal
- fish shell
- neovim (and vsnip)
- yabai
- skhd
- tmux

# Instructions

1. `git clone https://github.com/hkgnp/dotfiles.git`
2. Copy folders in `dotfiles` folder to `~/.config/`
